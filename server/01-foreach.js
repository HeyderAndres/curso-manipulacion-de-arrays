//es un metodo que itera un array y ejecuta una funcion por cada elemento del array.
//no retorna nada y no modifica el array original.
const letters = ['a', 'b', 'c', 'd', 'e'];

letters.forEach(item => console.log(item));